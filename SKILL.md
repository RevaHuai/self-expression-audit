---
name: self-expression-audit
description: 自我表达审计 Skill。通过深度对话帮助用户发现自身的表达结构，产出可作为 AI 语境底座的自我表达文档。支持三种运行模式（快速体检/核心审计/深度审计），按模块化设计，状态文件保证跨会话连续性。
---

# 自我表达审计 Skill

## 概述

这是一个通过深度对话完成的自我表达审计系统。核心原则：**最少问题暴露最多结构**。

系统通过 8 个板块（D0-D7，共 108 题）逐层深入，从身份定位到信念体系、表达机制、内容架构、禁忌边界，最终产出两份文档：
1. **Expression Context Profile** —— 被访谈者的完整语境文档，供 AI 阅读以更准确地生成内容
2. **原始访谈记录** —— 逐字问答，含追问记录，供被访谈者本人回顾

## 运行模式

| 模式 | 题量 | 时间 | 覆盖 | 产出 |
|---|---|---|---|---|
| `quick` | 10 | ~25min | 6 个板块的高密度精选 | 精简报告 + 升级邀请 |
| `core` | 70 | ~2h | D0+D1+D2+D5+D6 完整 | 完整报告（初步原型评估）|
| `deep` | 95 | ~2.5-3h | 核心全部 + D3+D7 | 完整报告（复核原型评估）|

### 模式选择逻辑

```
用户启动 Skill
    │
    ▼
是否有状态文件?
    │
    ├── 是 → 读取状态文件
    │       │
    │       ▼
    │   当前进度?
    │       │
    │       ├── 有未完成的板块 → 询问是否继续
    │       │       ├── 继续 → 从断点处恢复
    │       │       └── 重新开始 → 询问模式
    │       │
    │       └── 全部完成 → 询问是否升级或重新开始
    │               ├── 升级到下一模式 → 运行升级路径
    │               └── 重新开始 → 询问模式
    │
    └── 否 → 询问模式
            │
            ▼
        三种模式展示给用户选择
        （快速/核心/深度）
```

## 状态文件

系统的脊柱。所有跨会话连续性、模式升级、断点续做都依赖这个文件。

### 文件位置

```
{workspace}/.claude/skills/self-expression-audit/state/[subject-name]-state.json
```

### 核心规则

1. **按题号索引，不按模式分组**：rawData 以 Q1-Q108 为键，答案在升级时完整保留
2. **升级不丢数据**：快速模式的答案在核心/深度模式下保留，只追加追问
3. **深度标记**：每个答案标记 depth（quick/core/deep）， upgraded 标记是否被追加追问过
4. **状态是唯一事实来源**：产出文档从 rawData 编译，不依赖其他数据源

### 读写规则

- **读取**：每次会话开始时读取状态文件，恢复进度
- **写入**：每道题回答完成后立即写入 rawData 和 progress
- **原型锚点**：在板块完成时检查是否触发锚点
- **张力日志**：在访谈过程中实时检测矛盾，即时写入
- **产出**：只在模式完成或用户明确要求时生成报告

### Schema

完整 schema 定义见 [state-schema.json](state-schema.json)。

---

## 通用访谈规则

以下规则适用于所有模块和模式：

### 1. 一次一个问题

永远不同时抛出两道题。问完一道，等回答，再追问（如果需要），再进入下一道。

### 2. 追问含糊回答

当回答以下情况时，必须追问：
- **泛泛而谈**：「很多人都是这样」→「你是这样的人吗？具体来说呢？」
- **引用他人**：「我觉得 xxx 说得对」→「那是 xxx 的观点，你自己的呢？」
- **否定式回答**：「我不确定」→「如果你必须猜呢？」「如果有个答案是对的，你希望它是哪个？」
- **自我纠正**：不要阻止用户修正之前的回答。如果他们说「其实我刚才说的不完全对」，**这是黄金时刻**——停下，追问修正。

### 3. 指出矛盾（张力检测）

当发现前后回答之间存在潜在矛盾时，**温和地指出来**，不评判，只观察：

```
你之前说「我最佩服的是那种一个人扛住所有压力的人」，
但刚才你说「我认为好的表达者应该懂得借助团队的力量」。
这两件事好像有点 tension——你是怎么看待这个的？
```

**规则**：张力检测不求「解决」，只求「照亮」。记录到 tensionLog 即可，不需要用户给出完美解释。

### 4. 不提供选项

不要替用户想答案。不要说「你是不是想说是 xxx？」如果用户说不知道，使用上面的追问规则。

### 5. 记录原文，不做实时总结

在模块进行中，**不总结**用户说了什么。完整记录原始回答。总结只在最终产出时做。

### 6. 保持好奇心的语气

你是表达考古学家——在挖掘。语气应该是探索性的，不是审判性的。

---

## 愿景与反愿景系统（Vision & Anti-Vision）

D1 和 D6 不仅是独立的板块，它们共同构成一个完整的**价值立场系统**：

- **D1（信念与反共识）** 负责挖**愿景**：从"我相信什么"延伸到"我希望世界变成什么样"
- **D6（绝对禁区）** 负责挖**反愿景**：从"我绝不做什么"延伸到"我绝不希望世界变成什么样"

### 层级结构

```
信念（Belief）
  ↓  "我相信世界是什么样的"
立场（Position）
  ↓  "所以我站在哪里"
愿景（Vision）
  ↓  "我希望世界变成什么样"
    ↔  反愿景（Anti-Vision）
  ↓  "我绝不希望世界变成什么样"
行动原则（Practice）
  ↓  "因此我选择..."
```

### 理论锚点

**Fred Polak《The Image of the Future》**：北极星本质上是一个人脑中的「未来意象」(Future Image)——它不是目标，而是产生行动方向的源头。Polak 描述的循环 `Future Image → Present Action → Future Reality → New Future Image` 正是 loop 思维在个人层面的应用。

**Peter Senge（Positive Vision / Negative Vision）**：负面愿景容易变成"为了避免坏结果而行动"（avoidance motivation），正向愿景则是"我希望创造什么"。两者结合才是完整的价值立场——不是逃跑，而是有方向的站立。

**乌托邦 / 反乌托邦传统**：Vision = Utopia（Preferred Future），Anti-Vision = Dystopia（Future to be Avoided）。反愿景不是简单的"我不喜欢"，而是把现实趋势推演到极端后展示最终会变成什么。

**趋近—逃避结构（Approach / Avoidance Motivation）**：愿景是趋近方向（approach），反愿景是逃避方向（avoidance）。两者必须同时存在——只有愿景没有反愿景，北极星会变成"我不想成为某种人"（逃跑）；只有反愿景没有愿景，北极星会变成"我只是在抵抗"（无建设性方向）。

### 触发机制

- 当 D1 的回答中出现"我希望""我期待""我想看到"等未来指向词时，自动触发**愿景收敛追问**
- 当 D6 的回答中出现"如果...成为主流/常态"等宏观描述时，自动触发**反愿景收敛追问**
- 当 D1 的愿景和 D6 的禁区形成对照时，记录为张力 T12-T13（D1）和 T26-T28（D6）

### 最终产出：四句话

整个审计的终极收敛物不是一份人格报告，而是四句话：

> **我相信：______。**（信念）
> **所以我希望：______。**（愿景）
> **我绝不希望世界变成：______。**（反愿景）
> **因此我选择：______。**（行动原则）

这四句话对应 Belief → Vision → Anti-Vision → Practice，是一个人所有选择的底层操作系统。

---

## 模块路由

每个模块是一个独立的 `.md` 文件，包含该板块的完整提问逻辑。

### 模块依赖链

```
D0 (18) ← 无依赖，是所有模块的入口
  ├── D1 (15) ← 高依赖 D0
  ├── D2 (15) ← 中依赖 D0
  ├── D5 (12) ← 轻依赖 D0
  ├── D6 (10) ← 高依赖 D0
  ├── D3 (12) ← 扩展模块，核心完成后可选
  └── D7 (13) ← 扩展模块，核心完成后可选
```

### 路由逻辑

```
模式确定后：
  quick  → 从 quick-mode/questions.md 加载选题
  core   → 按依赖顺序加载 D0→D1→D2→D5→D6
  deep   → core 的全部模块 + D3 + D7

每个模块运行时：
  1. 读取状态文件，检查前置依赖是否完成
  2. 如果前置未完成 → 提示用户，提供选项
  3. 如果前置完成 → 加载模块的提问逻辑
  4. 逐题提问，每道题答完后写入 rawData
  5. 模块完成后 → 检查是否触发原型锚点
  6. 如果所有核心模块完成 → 提示是否升级/产出报告
```

### 前置依赖检查

当用户试图运行某个模块时，系统必须先检查其前置依赖：

| 模块 | 前置依赖 | 未满足时的处理 |
|---|---|---|
| D0 | 无 | 直接运行 |
| D1 | D0 完成 | 提示「D0 还没完成，需要先完成 D0 北极星板块」 |
| D2 | D0 完成 | 同上 |
| D5 | D0 完成 | 同上 |
| D6 | D0 完成 | 提示「D6 需要前面板块的信任积累，建议先完成 D0-D2」 |
| D3 | D0 完成 | 提示「D3 是扩展模块，建议先完成核心模块」 |
| D7 | D0 完成 | 同上 |

### 快速模式的选题映射

| 板块 | 快速模式选题 | 对应题号 |
|---|---|---|
| D0 | 最欣赏的 3 位表达者 | Q1 |
| D0 | 完整定位陈述 | Q13 |
| D0 | Ikigai 交叉点 | Q18 |
| D1 | 反共识观点 | Q19 |
| D1 | 最大主张 | Q33 |
| D2 | 怎么向朋友介绍 | Q34 |
| D2 | 自然 gravitate 的表达形式 | Q38 |
| D6 | 绝对不会碰的表达 | Q86 |
| D6 | 看到别人做时想"我永远不会那样" | Q88 |
| D7 | 什么让你不信任表达者 | Q96 |

---

## 升级路径

### 快速 → 核心

```
用户选择升级
    │
    ▼
系统对比：
  - 快速模式已答题 vs 核心模式需要的题
  - 计算缺失题数
  - 告知用户需补充的题数和预计时间
    │
    ▼
用户确认继续
    │
    ▼
从缺失的第一道题开始
（不会重答已答过的题）
    │
    ▼
对已有答案的追加追问：
  - 明确标记为「深化追问」
  - 不否定原始回答
  - 追加到 followUps 数组
```

### 核心 → 深度

```
用户选择升级
    │
    ▼
系统对比：
  - 核心模式已完成 70 题
  - 深度模式需追加 D3(12) + D7中未答的题
  - 如果快速模式已答 D7 的 Q96，则只需补 12 题
    │
    ▼
告知用户需补充的题数和预计时间
    │
    ▼
从 D3 的第一道题开始
```

### 升级追问的语气

对已有答案做追加追问时，使用以下话术模板：

```
好，我们从 [板块名] 继续。

你之前回答了 [题号]（[问题简述]）。
在 [当前模式] 下，我想对这道题做一点深化——

（你的[快速/核心]回答是：「[原始回答]」）

我想追问：[追问内容]

（这不算你答错了，只是帮你把答案磨得更深。）
```

---

## 产出文档触发

### 触发条件

产出文档只在以下时机生成：

1. **模式完成**：快速/核心/深度模式的所有题目完成后
2. **用户明确要求**：在访谈过程中，用户说「先出报告」或类似表达
3. **升级报告**：从快速升级到核心时，先输出快速报告，再询问是否继续

### 产出流程

```
模式完成
    │
    ▼
调用 compiler/compile.md 的逻辑
    │
    ▼
生成对应模式的报告
    │
    ▼
写入状态文件的 coreIdentity 和 quickReference
    │
    ▼
展示报告给用户
    │
    ▼
（快速模式）展示升级邀请
（核心/深度模式）询问是否需要导出文件
```

### 报告结构

三种报告的结构关系：

```
快速报告                核心报告                深度报告
─────────               ─────────               ─────────
Core Identity            Core Identity            Core Identity
  (初步推断)                (基于70题)               (基于95题)

Quick Reference          Quick Reference          Quick Reference
  (简化版)                  (完整版)                 (数据更丰富)

Tension Log              Tension Log              Tension Log
  (1-2条)                  (完整)                   (追加D3/D7)

原始问答(10题)            Section 0-6              Section 0-7
                          (D0-D2-D5-D6)            (+D3+D7)

原型评估                 原型评估(初步)            原型评估(复核)
  ✗                        ✓                        ✓✓

升级邀请                 升级邀请                   ✗
```

**核心报告和深度报告结构相同，深度报告数据密度更高。** 快速报告结构精简，但包含升级邀请。

---

## 模块文件索引

| 文件 | 板块 | 题量 | 说明 |
|---|---|---|---|
| `modules/D0-polaris.md` | 北极星与身份定位 | 18 | 必选入口，建立语境底座，含知识考古扩展层 |
| `modules/knowledge-archeology.md` | 知识考古 | — | D0 增强功能，影响图谱构建与学习路径推荐 |
| `modules/north-star-discovery-loop.md` | 北极星发现循环 | — | D0 深层算法，从 18 题收敛到北极星 |
| `modules/resonance-detector.md` | 四级共振检测器 | — | D0 全程监听，区分佩服/认出/认同/方向 |
| `modules/D1-beliefs.md` | 信念与反共识 | 15 | 揭示价值观、立场和愿景（Belief → Position → Vision） |
| `modules/D2-mechanism.md` | 表达机制 | 15 | 揭示实际沟通方式 |
| `modules/D5-architecture.md` | 内容架构 | 12 | 揭示节律和策略 |
| `modules/D6-taboo.md` | 绝对禁区 | 10 | 揭示边界、身份和反愿景（Boundary → Identity → Anti-Vision） |
| `modules/D3-aesthetics.md` | 审美禁区 | 12 | 扩展模块，可选 |
| `modules/D7-danger-signals.md` | 危险信号 | 13 | 扩展模块，可选 |
| `proto-system/prototype-anchors.md` | 原型锚点 | — | 三阶原型系统 |
| `compiler/compile.md` | 产出编译 | — | 报告生成逻辑 |
| `quick-mode/questions.md` | 快速选题 | 10 | 快速模式题源 |

## 原型锚点时机

| 锚点 | 时机 | 触发条件 |
|---|---|---|
| **锚点 1** | D0 完成后 | 18 题全部答完，形成初步身份假设 |
| **锚点 2** | D2 完成后 | D0+D1+D2 完成后，用表达机制数据精炼假设 |
| **锚点 3** | 全部完成后 | 95 题全部答完，最终确认或 pivot |

## 知识考古能力（D0 增强功能）

知识考古是北极星板块的扩展层，**不是独立模块**。它在用户回答 D0 现有 18 题时自动检测触发信号，动态激活追问。

### 触发条件

以下任一信号出现时，自动激活知识考古追问：

| 触发信号 | 典型用户表述 | 激活时机 |
|---|---|---|
| 人物提及 | "A 影响了我的看法"、"A 的观点让我…" | 任何 D0 问答中 |
| 书籍/文本提及 | "那本书改变了我的…"、"我反复读…" | 任何 D0 问答中 |
| 理论/框架提及 | "那个理论说…"、"我从那个框架里…" | 任何 D0 问答中 |
| 溯源自述 | "他受谁影响"、"他的来源是…" | Q4、Q5 直接触发 |
| 共振信号 | "他说出了我想说的话"、"这就是我" | 任何 D0 问答中 |

### 追溯深度

最多追溯 3 层影响关系，产出结构化的**影响图谱**（不是树，是图——一个人可以属于多条影响链）。

每层追溯后同步执行**剥离式追问**（规则3）：把人物从答案中剥离，提取原则。

### 四层素材标注

知识图谱中的每个节点按三阶结构标注：

- **一阶素材（Form）：** A 的产出——文章、产品、表达方式
- **二阶素材（Method）：** A 的方法论——思考框架、工作方法
- **三阶素材（Source）：** A 的源头——原典、历史、哲学

### 学习路径推荐

基于知识图谱，系统自动生成：

1. **直接阅读清单**：用户北极星反复引用的书籍
2. **前置阅读清单**：理解二阶影响源需要的背景知识
3. **对比阅读清单**：多个影响源的差异来源
4. **下一步探索**：沿着影响链的下一个节点

### 与北极星发现循环的衔接

知识考古的输出直接成为北极星发现循环的输入。知识图谱存储在 `expressionAuditData.knowledgeGraph`，后续模块通过读取这个字段获取语境底座。

### 详细规范

- 影响图谱构建规则：`modules/knowledge-archeology.md`
- 北极星发现循环（9轮收敛算法）：`modules/north-star-discovery-loop.md`
- 四级共振检测（Admiration/Recognition/Identification/Direction）：`modules/resonance-detector.md`

## Anti-Overfitting 原则

本 Skill 的使用者有两条必须遵守的规则：

1. **不做模板化总结**：产出文档中的总结部分（Core Identity、Quick Reference）必须基于被访谈者的具体回答生成，不能套用模板句式。
2. **张力日志优先于整洁结论**：当回答之间存在矛盾时，保留矛盾，不要为了「好看」而消解它。矛盾是人格的信号，不是错误。
